export const selectIsOpen = ({ editModal }) => editModal.isOpen;
export const selectEventId = ({ editModal }) => editModal.eventId;
export const selectPreselectedDate = ({ editModal }) =>
  editModal.preselectedDate;
